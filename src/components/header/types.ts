export interface HeaderPropsTypes {
  screenTitle?: string;
  backIcon?: boolean;
  onPressBellIcon?: any;
  icon?: any;
  paddingHorizontal?: any;
  descriptionText?: string;
  description?: boolean;
}
